extends layout

block content
  h1= article.title
  h2 Written by #{article.auther}
  p= article.body
  hr
  a.btn.btn-default.bg-light.mr-2(href='/articles/edit/'+article._id) Edit
  a.btn.btn-danger.delete-article(href='#',data-id=article._id) Delete